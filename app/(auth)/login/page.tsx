import { LoginForm } from "@/features/auth/login-form";

export const metadata = {
  title: "Sign In | PayCore",
  description: "Sign in to PayCore",
};

export default function LoginPage() {
  return <LoginForm />;
}
